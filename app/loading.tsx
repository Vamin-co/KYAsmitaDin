import { LoadingSplash } from "@/components/LoadingSplash";

// Root-level fallback: the branded splash for the initial load and the login route.
export default function Loading() {
  return <LoadingSplash />;
}
